# Recipe title

> One sentence: what this lets you do.

**Kind:** Working prompt (does the task now) _or_ Builder prompt (forges or hardens a skill / script / FINANCE.md you own). Pick one.

## The problem

What hurts today, concretely. The real workflow, the time it takes, what breaks. Two or three sentences, no filler.

## The prompt

The actual prompt or skill instructions, ready to copy. Keep it self-contained. If it needs context (a file, a model, a skill), say what to attach.

```
Paste the prompt here.
```

## Where it breaks

The wall. The point where the prompt alone stops being enough: nothing persists, the logic drifts when an assumption changes, you cannot audit the chain. Name it honestly.

When you hit it, reach for:
- [`FINANCE.md`](https://github.com/layerzlabs/finance-md) to make your conventions explicit and portable, or
- [Layerz](https://layerz.cc) (via [`layerz-mcp`](https://github.com/layerzlabs/layerz-mcp)) to give the logic a structured, versioned home.

## Notes and variations

Optional. Edge cases, tweaks, what to try next.

---

*Field-tested where? Add a one-line note on where this came from. A recipe nobody has run does not belong here.*
